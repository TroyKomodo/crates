syntax = "proto3";

package error_states.cel;

import "tinc/annotations.proto";

message MessageCelResolve {
  string x = 1;
  option (tinc.message).cel = {
    expression: "input.no_such_field == \"\""
    message: "bad member"
  };
}
